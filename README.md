Character or object 角色或对象
Number of frames 帧数
Animation type (walk, run, jump, idle, attack)
动画类型（行走、奔跑、跳跃、待机、攻击）
Camera angle (side-view, top-down, 3/4 view)
相机角度（侧视图、俯视图、3/4 视图）
Layout (horizontal strip, vertical strip, 4×4 grid)
布局（水平条带，垂直条带，4×4 网格）
Transparent background 透明背景

Create a walk cycle of a hero character. 6-frame side-view, Horizontal sprite sheet. Transparent background. Consistent proportions.
“创建一个英雄角色的 6 帧侧视图行走循环。水平精灵表。透明背景。保持一致的 proportions。”

根据上面生成的图片，创建一个沿着 Z 轴上下旋转的金币 spritesheet 图，每 45 度一帧， 8-frame side-view, Horizontal sprite sheet. Transparent background. Consistent proportions.

生成一张像素风的金币图片，一面是$符号，一面是骷髅头。 64x64 pixels. Transparent background.

生成一张像素风的沿着 Z 轴上下旋转的金币的 sprite sheet 图，每 45 度一帧， 6-frame side-view, Horizontal sprite sheet. Transparent background. Consistent proportions.
